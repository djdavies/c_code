#include <stdio.h>

int main (int argc, char *argv[]) {
  int intarray[5];
  int l;

  for (l = 0 ;l < 5 ; ++l) {
    printf ("Array element %d is at address %p\n", l, &(intarray[l]));
  }

  return 0;
}
